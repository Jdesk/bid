machine:
  node:
    version: 4.2.2
  services:
    - cassandra
    - elasticsearch
    - redis-server
    - mongodb
    checkout:
  post:
    - git submodule sync
    - git submodule update --init
dependencies:
  override:
    - npm install
